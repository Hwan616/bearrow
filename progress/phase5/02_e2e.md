# 5.2 E2E 스모크 테스트

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-06-29 16:24 |
| 상태 | ✅ 완료 |
| 요구사항 | NFR-REL-001 |

## 구현 내용

- `@testing-library/react-native` (v14) devDependency 추가
- `__tests__/App.smoke.test.tsx` — App 셸 수준 스모크 테스트 9개
  - 앱 초기화: 마이그레이션 전 탭바 미표시 / 완료 후 탭바 3개 표시
  - 탭 전환: 기본 캘린더 선택 / 할일·설정 탭 전환 / 복귀
  - FAB: 캘린더·할일 탭 FAB 존재 확인 / 설정 탭 FAB 없음 확인
- `App.tsx` — `testID` 추가 (tab-calendar, tab-todo, tab-settings, fab-add-event, fab-add-todo)

## 주요 결정

- feature 컴포넌트(MonthView, TodoList 등)는 모킹하여 App 셸 플로우에만 집중
- RNTL v14 + React 19 에서는 `act(async () => { render(); await Promise.resolve() × 3 })` 패턴으로 마이크로태스크 플러시 보장
- `testID` 기반 쿼리 사용 — 이모지 포함 탭 텍스트·accessibilityLabel 매칭보다 안정적

## 남은 과제 / 주의사항

- SafeAreaView deprecated 경고는 테스트 실패와 무관 (react-native 0.81의 알려진 경고)
- 실기기/시뮬레이터 레벨 E2E(Detox 등)는 Phase 6 이후 필요 시 별도 추가
