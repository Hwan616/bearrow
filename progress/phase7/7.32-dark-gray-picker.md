# 7.32 다크 모드 회색 밝게 + 일정 편집 시각 피커 다크 대응

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-08 02:58 |
| 커밋 | d8b4eb1 |

## 한 일
- 다크 모드 배경·surface 회색을 한 단계 밝게 조정.
  - `background.primary` #212529→#2A2E33, `secondary`/`surface.default` #343A40→#3C4247, `tertiary`/`surface.raised` #495057→#545B62.
- 배경이 밝아진 만큼 묻힐 수 있는 `text.disabled`도 밝게(#6C757D→#8B929A) 조정해 가독성 예방.
- EventForm `DateTimePicker`(시작/종료·반복 종료 날짜)에 `themeVariant`(dark/light)·`accentColor` 전달 → 다크 모드에서 라이트 스타일로 보여 안 보이던 문제 해결.

## 파일
- `src/theme/tokens.ts`, `src/features/calendar/components/EventForm.tsx`

## 남은 과제 / 주의사항
- `themeVariant`는 네이티브 피커 속성 → 시각 확인은 시뮬레이터/기기 다크 모드에서 필요.
