# 7.20 중간 정리 — PlusIcon 공용 추출 · BACKLOG · progress 보완

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-07 19:57 |
| 커밋 | f64c029 |

## 한 일

### 코드
- `App.tsx`와 `CategoryManager.tsx`에 중복 정의된 `PlusIcon` 컴포넌트를 `src/ui/Icons.tsx`로 추출
- 두 파일 모두 `import { PlusIcon } from "@/ui/Icons"`로 교체
- GearIcon·ChevronLeft는 App.tsx에서만 사용되므로 이동하지 않음

### 문서
- `docs/BACKLOG.md`: 7.14 이후 누락된 7.15~7.20 항목 추가(모두 `[x]` 완료)
- `progress/INDEX.md`: 7.15~7.20 목차 행 추가
- `progress/phase7/7.15~7.19` 파일들: 이번 정리 작업에서 INDEX만 추가 (개별 파일은 필요 시 작성)

## 주요 결정

`PlusIcon`은 pixel-perfect 계산 로직을 가진 컴포넌트라 두 곳에 흩어지면 한쪽만 수정될 위험이 있었음.
`src/ui/` 폴더가 이미 공용 UI 컴포넌트 자리(`AppBottomSheet`, `AppSidePanel`)이므로 `Icons.tsx`를 추가하는 것이 자연스러운 위치.

## 남은 과제 / 주의사항

- 향후 아이콘 추가 시 `src/ui/Icons.tsx`에 export 추가
- GearIcon·ChevronLeft는 App.tsx 내부 전용이므로 현재 위치 유지
