# 7.30 캘린더·카테고리·하단바 스타일 8종 개선

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-08 02:45 |
| 커밋 | af44641 |

## 한 일 (8종)
1. Month View 일정 박스 모서리 2→3, **글씨(검정/흰색) 위에 카테고리 색 박스를 덧입히는** 레이어링으로 변경.
2. 카테고리 추가 버튼 아이콘을 하단바 `PlusIcon`과 통일.
3. 뒤로가기 꺾쇠를 정사각형 테두리 45° 회전 방식(단일 miter join)으로 교체.
4. Year View 연도 제목·구분선을 미니 달력 콘텐츠와 동일한 베젤 여백으로 정렬.
5. 하단 바에 배경색 **그라데이션 스크림**(아래로 갈수록 진해짐) 추가 — 버튼 가독성 확보. (실제 블러는 네이티브 모듈 필요, 근사 구현)
6. 카테고리 색상 팔레트를 **형광색 계열**로 전면 교체 + 기본 색상 갱신.
7. Day View '종일'과 날짜 레이블 사이 회색 구분선 제거.
8. Day View 고정 날짜 헤더 배경을 라이트·다크 모두 회색(`background.tertiary`)으로 통일.

## 파일
- `App.tsx`, `src/features/calendar/components/MonthView.tsx`, `src/features/calendar/components/YearView.tsx`, `src/features/calendar/components/DayView.tsx`, `src/features/category/components/CategoryManager.tsx`, `src/features/category/types.ts`, `src/features/category/api/categories.ts` + 테스트

## 남은 과제 / 주의사항
- 팔레트 변경은 색상 선택지·신규 기본값에만 적용. 기존 카테고리는 저장된 색 유지 → 재편집 필요.
- 하단바 블러는 그라데이션 스크림으로 근사. 실제 유리 블러가 필요하면 `expo-blur` 도입 검토.
