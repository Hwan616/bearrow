# 7.27 일정 편집 UX 개선 (바 탭 직접 편집·섹션·라이트 모드)

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-07 23:33 |
| 커밋 | 8b25ebf |

## 한 일
- Month/Day View에서 일정 바를 탭하면 `EventDetailSheet`를 거치지 않고 **편집 화면(EventForm)이 바로** 열리도록 변경. 공휴일 바는 제외.
- EventForm 섹션 재구성: (제목) / (종일·시작·종료) / (알림·반복) / (메모).
- 라이트 모드 섹션 배경을 `background.tertiary`(회색)로 지정해 다크 모드처럼 영역 구분.
- `+x개` 초과 표시를 마지막 visible 바 바로 아래(상단 기준)로 이동.
- 스모크 테스트 목을 `EventDetailSheet` → `EventForm`으로 교체.

## 파일
- `App.tsx`, `src/features/calendar/components/MonthView.tsx`, `src/features/calendar/components/EventForm.tsx`, `__tests__/App.smoke.test.tsx`
