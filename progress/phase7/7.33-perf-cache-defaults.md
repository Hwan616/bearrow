# 7.33 성능·UX (월 캐시·리스트 튜닝·기본 시각 5분·알림/반복 분리)

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-08 03:11 |
| 커밋 | 30600d9 |

## 한 일
- **월별 캐시**(`useMonthItems`): 빠른 스크롤 재방문 시 마지막 조회 결과를 즉시 렌더(로딩 깜빡임 제거) + 백그라운드 재조회. 데이터 변경 시 `bumpCalendar`→`invalidateMonthItemsCache`로 무효화.
- **FlatList 튜닝**: Month `windowSize` 5→9, Year 3→5, `maxToRenderPerBatch`·`updateCellsBatchingPeriod`·`initialNumToRender` 지정으로 스크롤 버벅임 완화.
- 신규 일정 기본 시작 시각을 **가장 가까운 5분 단위**로, 종료는 시작 +1시간.
- EventForm에서 **알림·반복을 별도 영역**으로 분리.

## 파일
- `src/features/calendar/hooks/useMonthItems.ts`, `src/features/calendar/components/MonthView.tsx`, `src/features/calendar/components/YearView.tsx`, `src/features/calendar/components/EventForm.tsx`, `src/features/calendar/utils/eventFormUtils.ts`, `App.tsx` + 테스트

## 남은 과제 / 주의사항
- 로컬 SQLite 조회 자체는 빠름 → 시뮬레이터 버벅임은 상당 부분 시뮬레이터 성능 영향. 실기기 확인 권장.
