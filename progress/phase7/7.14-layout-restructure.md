# 7.14 레이아웃 전면 재구성

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-07 05:41 |
| 커밋 | 37b36e1~fbc57d4 (레이아웃 재구성 일괄) |

## 한 일

### 뷰별 상하 레이아웃 재설계

**Year View**
- NavBar: `BeArrow`(좌) / 검색·설정(우)
- Footer: `오늘` 버튼만 (pill 스타일)

**Month View**
- NavBar: `< YYYY년`(좌) / 검색·설정(우)
- SubTitle: `MM월` 큰 글씨 (탭 → YearMonthPicker 열림)
- Weekday bar: 일~토 고정 (App.tsx 레벨, 스크롤 안 됨)
- MonthItem에서 월 타이틀·요일행 제거 → `MONTH_ITEM_HEIGHT = 540`
- Footer: 오늘 / 할 일 / ＋

**Day View**
- NavBar: `< M월`(좌) / 검색·설정(우)
- 날짜 헤더 타이틀 (`YYYY년 MM월 DD일 W요일`)를 App.tsx 레벨로 이동
- DayView 내부 dateHeader 제거
- Footer: 오늘 / 할 일 / ＋

### 공통 변경사항
- 설정 버튼: 푸터 → 헤더 우측
- 추가(+) 버튼: 헤더 → 푸터 (Month/Day만)
- 하단 툴바 배경: 투명
- 버튼 스타일: 회색 pill (`rgba(120,120,128,0.12)`)

### App.tsx
- `visibleDayDate` 상태 분리 (헤더 표시용 vs `selectedDate` — 무한 리렌더 방지)
- `visibleMonthYear` / `visibleMonthMonth` 상태로 MonthView 스크롤 위치 추적
- `YearMonthPicker`를 MonthView → App.tsx 레벨로 이동
- `MonthViewHandle` / `useRef` + `useImperativeHandle`로 `scrollToMonth` 외부 노출
- `goToToday()`: Year→Month 이동, Month 유지, Day 유지 분기
- `formatDayHeaderTitle` export → App.tsx에서 import 사용

### 테스트 수정
- `DayView.test.tsx`: `day-header-title` testID가 DayView에 없어진 테스트 제거
- `MonthView.test.tsx`: MonthItem 월 타이틀 제거로 `getByText("2026년 7월")` 제거
- `App.smoke.test.tsx`: DayView 목에 `formatDayHeaderTitle` 추가

## 주요 결정

- `visibleDayDate`를 `selectedDate`와 분리하여 `onDateChange` 콜백이 DayView key를 건드리지 않도록 설계 → 무한 remount 방지
- `onVisibleMonthChangeRef` 패턴으로 stale closure 방지
- MonthItem 높이를 544→540으로 정밀 조정 (헤더 44 + 요일행 24 제거)

## 남은 과제 / 주의사항

없음
