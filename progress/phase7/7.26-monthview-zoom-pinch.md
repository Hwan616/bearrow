# 7.26 MonthView 6단계 이산 줌 + 핀치 확대/축소

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-07 23:18 |
| 커밋 | 76711d5, 8f271c4 |

## 한 일
- Month View에 **6단계 이산 줌** 도입. 축소→확대 순서로 표현 방식이 바뀜.
  - Level 0 `dots`(점) → 1 `thin_bars`(얇은 막대) → 2~5 `bars`(박스, maxTracks 2·3·4·5).
  - 각 레벨의 `weekRowHeight`를 정의(60/77/84/100/116/132), 최대 확대 시 일정 5개 표시.
- 핀치 제스처로 셀 높이를 **연속적으로** 변경하고, 손을 떼면 **가장 가까운 레벨에 스냅**.
- FlatList 레이아웃(`stableWeekRowHeight`)과 렌더 높이(`renderWeekRowHeight`)를 분리해 스크롤 점프 억제.

## 파일
- `src/features/calendar/components/MonthView.tsx`, `src/config/layout.ts`, `package.json`(gesture-handler jestSetup)

## 비고
- 이후 7.35에서 앵커 고정·실시간 상세도 전환·줌 영속으로 전면 재설계.
