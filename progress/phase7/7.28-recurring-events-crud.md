# 7.28 반복 일정 기능 강화 + CRUD 버그 수정

| 항목 | 내용 |
|---|---|
| 날짜 | 2026-07-08 01:50 |
| 커밋 | 2b71135 |

## 한 일

### 반복 기능 (Apple 캘린더 참고)
- 반복 주기 확장: 매일·매주·**2주마다**·매월·**매년**.
- 반복 종료 조건 추가: 종료 안 함 / **N회 후** / **특정 날짜까지**.
- 반복 일정 편집·삭제 시 **범위 선택 다이얼로그**: 이 이벤트만 / 이 이벤트 및 이후 / 모든 이벤트.
- `events.ts` 헬퍼 추가: `createExceptionEvent`·`deleteExceptionInstance`·`editAllRecurring`·`splitAndEditThisAndFollowing`·`deleteThisAndFollowing`·`deleteAllRecurring`.
- `rruleUtils`: `parseRRuleToOption`·`parseRRuleEnd`·`stripRRuleEnd` + biweekly/yearly/종료조건 빌드·파싱.

### CRUD 버그 수정
- `makeDefaultValues`가 편집 시 `recurrence`·`reminderMinutes`를 하드코딩(none/null)해 저장 시 반복·알림 정보가 소멸되던 문제 → rrule/이벤트에서 복원.
- 가상 반복 인스턴스(`masterId:utcMs`)를 편집·삭제할 때 올바른 마스터를 찾아 처리.
- EventForm에 삭제 버튼 추가.

### 기타
- Month View 일정 바 좌우 여백 1pt 축소.

## 파일
- `src/features/calendar/api/events.ts`, `src/features/calendar/utils/rruleUtils.ts`, `src/features/calendar/utils/eventFormUtils.ts`, `src/features/calendar/components/EventForm.tsx`, `src/features/calendar/components/MonthView.tsx`, `App.tsx` + 관련 테스트

## 남은 과제 / 주의사항
- 이미 잘못 저장된 기존 일정(반복·알림 소실분)은 코드 수정으로 소급 복구되지 않음 → 재편집 필요.
