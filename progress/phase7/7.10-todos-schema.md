# 7.10 todos 스키마 변경·마이그레이션

**완료일시**: 2026-07-06 00:00

## 변경 내용

### 스키마 (`src/db/schema.ts`)
- `assignedDate` (int, timestamp mode, notNull) 추가 — 캘린더 표시 기준 날짜
- `hasDueTime` (int, boolean mode, notNull, default false) 추가 — dueDate에 시간 정보 포함 여부

### 마이그레이션 (`src/db/migrate.ts`)
- `0008_add_assigned_date_to_todos` 추가
  - ALTER TABLE 로 두 컬럼 추가
  - 기존 데이터 백필: `assigned_date = COALESCE(due_date, created_at)`, `has_due_time = CASE WHEN due_date IS NOT NULL THEN 1 ELSE 0 END`

### API (`src/features/todo/api/todos.ts`)
- `updateTodo`: dueDate 변경 시 assignedDate·hasDueTime 자동 동기화
- `getTodosByDueDateRange`: dueDate 대신 assignedDate 기준으로 범위 조회
- `createTodoFromEvent`: assignedDate·hasDueTime 필드 추가

### 훅 (`src/features/todo/hooks/useTodos.ts`)
- `handleCreate`: assignedDate = dueDate ?? now, hasDueTime = dueDate != null

### 동기화 (`src/sync/pull.ts`)
- `ServerTodoRow` 타입에 `assigned_date`, `has_due_time` 추가
- INSERT OR REPLACE SQL에 두 컬럼 반영

### 테스트 픽스처 갱신 (7개 파일)
- `todos.test.ts`, `TodoList.test.tsx`, `TodoSheet.test.tsx`
- `useMonthItems.test.ts`, `useDayItems.test.ts`, `todoListUtils.test.ts`
- `migrate.test.ts` — execAsync 카운트 8→9, 0008 마이그레이션 검증 추가

## 검증
- `typecheck` ✅ | `lint` ✅ | `test` ✅ (444 passed)
